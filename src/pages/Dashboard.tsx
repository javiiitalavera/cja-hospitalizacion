// El Dashboard vive ahora en su propia carpeta modular
// (src/pages/dashboard/), separado en piezas independientes en vez
// de un único archivo — este reexport evita tocar el resto del
// enrutado de la aplicación.
export { Dashboard } from './dashboard/Dashboard'
